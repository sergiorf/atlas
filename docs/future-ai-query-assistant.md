# Future AI query assistant

AI is post-MVP, paid/gated functionality. It should translate natural language into a validated JSON query intent, never arbitrary SQL. The backend owns provider credentials and enforces authorization, filters, limits, and cost controls; provider keys must never reach a browser.
